# Python related
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.venv/
venv/
env/

# Backend Persistent Data (do NOT commit these)
# These directories are created by your application for transcripts and vector store.
# They should NOT be committed to Git. Docker volumes will handle persistence.
Code/backend/transcripts/
Code/backend/vectorstore/

# Environment variables
.env
.env.*.local

# Docker related (ignore temporary files, but NOT Dockerfiles or docker-compose.yml)
# The .dockerignore files themselves should be committed.
# Docker Desktop specific files
.docker/
docker-compose.override.yml # If you use an override for local dev

# VS Code Dev Containers related (the .devcontainer/ folder itself SHOULD be committed)
# But ignore user-specific or temporary files within it if necessary
# .devcontainer/*.backup
# .devcontainer/.devcontainer.log

# Operating System generated files
.DS_Store # macOS
Thumbs.db # Windows
desktop.ini # Windows

# Generic log files
*.log